model:
  name: "LogisticRegression"
  type: "logistic_regression"

parameters:
  penalty: "l2"
  C: 1.0
  solver: "lbfgs"
  max_iter: 1000
  random_state: 42

training:
  cross_validation: true
  cv_folds: 5

mlflow:
  log_model: true
  register_model: true
  model_name: "logistic_regression_iris"
